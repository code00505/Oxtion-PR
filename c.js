function sendData() {
    // Assuming an asynchronous function to send data
    // Here, you might have an AJAX request or other sending process
    // After the data is successfully sent, add a class to the button to trigger CSS changes
    document.getElementById("sendButton").classList.add("sent");
  }